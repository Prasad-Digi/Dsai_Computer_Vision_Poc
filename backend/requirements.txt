# ---- Core API ----
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
pydantic==2.10.3
pydantic-settings==2.6.1

# ---- Computer Vision ----
ultralytics>=8.4.0       # 8.4.x+ ships YOLO26 + YOLOE-26 (open-vocabulary) support; older versions do NOT have these
lap>=0.5.12               # required by BoT-SORT tracker (linear assignment) used in services/detector.py
clip @ git+https://github.com/ultralytics/CLIP.git   # required by YOLOE-26's text-prompt embeddings (attribute_classifier.py)
opencv-python-headless==4.10.0.84
numpy==1.26.4
imageio-ffmpeg==0.5.1     # bundles a static ffmpeg binary - used to transcode annotated output to browser-playable H.264 (no system ffmpeg install needed)

# ---- Utilities ----
python-dotenv==1.0.1
loguru==0.7.2
aiofiles==24.1.0